# 在线判题系统验收报告

日期：2026-05-26

## 1. 验收范围

本次自动化验收覆盖以下内容：

- 项目单元测试
- 服务健康检查与核心 API 烟雾测试
- 并发提交性能测试

## 2. 自动化测试结果

### 2.1 单元测试

- 命令：`RunCtest_CMakeTools`
- 结果：通过
- 统计：`1/1` tests passed

### 2.2 服务健康检查

- 命令：`curl -s -w '\nHTTP_CODE:%{http_code}\n' http://127.0.0.1:8080/healthz`
- 结果：通过
- 返回：`ok`，HTTP `200`

### 2.3 前端相关 API 烟雾测试

- 命令：`BASE_URL=http://127.0.0.1:8080 tests/frontend/api_smoke_check.sh`
- 结果：部分通过
- 通过项：
  - `healthz`
  - `problems list has A+B`
  - `problems list has Big Integer`
  - `problem detail has samples`
- 失败项：
  - `problem detail xss text stored`
- 说明：该项失败表现为精确字符串未匹配，现象更像是题面文本的转义或换行差异，而不是接口完全不可用。

### 2.4 并发性能测试

- 命令：`python3 tests/perf/perf_test.py`
- 结果：未完成收敛，无法产出有效 p50/p90/p95/p99
- 当前观察：
  - 脚本已启动 50 并发提交
  - 数据库中存在明显提交积压，查询时可见 `pending` 数量较多
  - 这次压测未能稳定结束，因此无法据此证明 `100` 并发下 `95%` 简单题在 `3s` 内返回结果

## 3. 结论

当前项目已完成基础自动化验证，但**性能验收未通过**，原因是并发测试未收敛并且无法产出满足 SPEC 的统计证据。核心功能链路可用性已通过单测与健康检查验证，但烟雾测试存在一处文本断言失败，性能项仍需要进一步稳定判题积压与测试脚本后再次测量。

## 4. 后续建议

1. 清理/隔离历史 `pending` 提交后重新跑压测，避免历史积压干扰结果。
2. 给压测脚本增加总超时与分批统计，确保能稳定产出分位数结果。
3. 针对题面 XSS 失败项，确认后端返回的转义策略与测试断言是否一致。
